spring:
  application:
    name: DineFlow
#  datasource:
#    url: jdbc:mysql://localhost:3306/dineflow_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#    username: root
#    password: ku193ttudm
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    locations : classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8050
spring:
  application:
    name: DineFlow
#  datasource:
#    url: jdbc:mysql://localhost:3306/dineflow_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#    username: root
#    password: ku193ttudm
  security:
    user:
      name: user
      password: user123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations : classpath:db/migration
    baseline-on-migrate: true

#security:
#  jwt:
#    secret: nvjfenvjnjv53352434rnnc19dnwqdneciu439jn
#    expiration-ms: 3600000

server:
  port: 8050